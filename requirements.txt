# LocalPodcastLLMStudio - Runtime Dependencies
# Universal 100% Local Two-Host Podcast Generator

# Core Desktop GUI Framework (Windows 11 Fluent Dark UI)
customtkinter>=5.2.0

# Local Neural Voice Synthesis (Piper TTS / Local ONNX, Zero Cloud Data Sharing)
# Piper runs locally; edge-tts can be installed optionally if needed

# Pure Python Document Parsing (.pdf document ingestion)
pypdf>=4.0.0

# HTTP Client for Local Ollama REST API Communication
requests>=2.31.0

# Terminal UI Framework (Tokyo Night TUI, ANSI styling & components)
rich>=13.0.0
